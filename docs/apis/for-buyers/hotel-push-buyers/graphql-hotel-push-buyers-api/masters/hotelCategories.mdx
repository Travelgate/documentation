---
sidebar_position: 2
---

import {hotelCategoriesQuery, hotelCategoriesVariables, hotelCategoriesByCategoryIdsVariables} from "../../../../../../src/graphql/inventory-push/master/hotelCategories.query";


# Hotel Categories

The hotelCategories Query returns a list of the categories that a hotel can have assigned in Inventory. The returned fields include:

* `id`
* `name`


## Hotel Categories Inputs

When creating your hotelCategories query, you have 1 input to fill based on your specific needs:

1. Criteria


### 1. Criteria   

This query offers versatility, with an optional field `categoryIds` that can be filled with the categories to retrieve. This flexibility empowers you to create a personalized hotelCategories Query, tailoring the requested fields to your specific needs. Consequently, the response will only include information that is highly relevant to you, streamlining the retrieval process and ensuring the data you receive meets your exact requirements.

**Optional input**
* `categoryIds`

### Requests Examples

#### Search for full list of Hotel Categories available

<GraphqlSample query={hotelCategoriesQuery} variables={hotelCategoriesVariables} />


#### Search for a specific list of Hotel Categories

When utilizing the hotelCategories Query, for more personalized results, filter specific categories based on your interests. For example, to retrieve only the Hotel Categories with starts, add the `categoryIds` field with the following codes in your input:

<GraphqlSample query={hotelCategoriesQuery} variables={hotelCategoriesByCategoryIdsVariables} />