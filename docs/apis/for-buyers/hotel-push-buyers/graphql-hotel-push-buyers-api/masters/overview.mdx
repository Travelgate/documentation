---
sidebar_position: 1
---

# Overview

The Master methods provide access to essential static information from Inventory. 
To effectively manage these methods, familiarize yourself with calls such as `hotelCategories`, `localities`, `mealplans`, `roomsMaster` and `hotelsMaster`.

import DocCardList from '@theme/DocCardList';
import {useCurrentSidebarCategory} from '@docusaurus/theme-common';

<DocCardList items={useCurrentSidebarCategory().items.filter((item, i) => i > 0)}/>